#include <stdio.h>
#include <stdlib.h>

int main()
{
  int a, *p;
  printf("Enter the Integer: ");
  p = (int *)malloc(sizeof(int));
  scanf("%d", p);
  printf("Address = %d    Integer = %u", *p, p);
  return 0;
}
